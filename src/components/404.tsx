import React from "react";

const NotFoundText = () => {
  return (
    <h1 className={`text-7xl font-semibold text-center text-gray-900`}>
      4
      <span className="relative">
        <span className="absolute h-3 w-3 bg-black top-[45%] left-[38%] rounded-full" />
        0
      </span>
      4
    </h1>
  );
};

export default NotFoundText;
